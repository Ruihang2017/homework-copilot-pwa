# Simple nginx config for frontend-only container
# This gets overwritten when using the main nginx container
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Handle SPA routing - return index.html for all routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}
